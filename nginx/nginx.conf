events {
    worker_connections 1024;
}

http {
    # Gelen trafiğin gerçek IP'sini ve host bilgisini loglamak için
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    server {
        listen 80;
        server_name localhost; # İleride buraya ids.cagla.com gibi domain gelecek

        # Dışarıdan gelen her isteği senin Python Backend'ine yönlendiriyoruz
        location / {
            proxy_pass http://ids_backend:8000; 
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}